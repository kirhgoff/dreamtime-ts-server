{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgis",
  "password": "postgis",
  "database": "gis",
  "synchronize": true,
  "logging": false,
  "entities": ["src/models/**/*.ts"],
  "migrations": ["migration/*.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
